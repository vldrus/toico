A simple command line utility which converts image files to PNG-based [ICO](https://en.wikipedia.org/wiki/ICO_(file_format))

Usage:

```
Usage: toico <flags> <input_file>
Flags:
  -o string
        output file name (default "test.ico")
```
